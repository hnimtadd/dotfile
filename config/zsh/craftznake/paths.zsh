# configure your path here, if you want to append any path to PATH, try to reuse append_path_if_not_exists.
# This will check if your path is already in PATH, if not, then append.
# e.g
# append_path_if_not_exists "$HOME/.scripts"
# append_path_if_not_exists "$HOME/local_bin"
